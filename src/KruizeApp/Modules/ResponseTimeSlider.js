import React, {useState} from 'react';
import KruizeSlider from "../Components/KruizeSlider";

const ResponseTimeSlider = () => {

    return (
        <KruizeSlider title="Response Time" />
    )
};

export default ResponseTimeSlider;